"use strict"; // treat all JS code as newer version of Javascript
// alert("Hello World") Here this command will not work since we are using nodejs not browser
console.log(2 + 2);

// Datatypes in Javascript
let userName = "Raghav Dolyar" // string
let age = 18 // number
let isLoggedIn = true // boolean
isLoggedIn = false
let x = null // null is a standalone value
let y = undefined;
let z = Symbol // used for uniqueness
console.table([userName, age, isLoggedIn, x, y, z])

// exceptions in JS
console.log(typeof null);
console.log(typeof undefined);
